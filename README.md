# rtmx

The idea explored here:

htmx allows us to do network requests on any dom element. So I thought may be, when  
I clicked on a button, it would dispatch a get request to a /todo route which has a  
react renderToString html content and when I get back the response I could swap it with a provided root element all by using htmx. But it does not work.
